[project]
name = "embedding"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "gensim>=4.4.0",
    "numpy>=2.4.6",
    "scipy>=1.17.1",
    "sentence-transformers>=5.5.1",
    "tokenizers>=0.22.2",
]
